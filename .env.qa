# QA environment configuration for subscription-web
# OAuth Authority: commented to use dynamic fallback based on window.location.origin
# VITE_OAUTH_AUTHORITY=http://test.orkidea.io:8080

# OAuth Configuration
VITE_OAUTH_CLIENT_ID=subscription-ui
VITE_OAUTH_REDIRECT_URI=http://test.orkidea.io/subscription/callback
VITE_OAUTH_POST_LOGOUT_REDIRECT_URI=http://test.orkidea.io/subscription

# API URLs for QA (if needed)
VITE_API_URL=http://test.orkidea.io:8080
