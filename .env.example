# OIDC authority — leave empty to auto-detect (localhost → http://localhost:8085)
# VITE_OAUTH_AUTHORITY=http://localhost:8085

# API gateway
VITE_API_URL=http://localhost:8080

# OAuth client
VITE_OAUTH_CLIENT_ID=subscription-ui

# Temporary: org ID until multi-org flow (step 11.5.15)
# VITE_ORG_ID=<your-org-uuid>
